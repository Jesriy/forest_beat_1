{
  "name": "forest-rhythm",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "纯前端网页音游：树林 / 城市主题切换 + 4 轨下落判定",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "node scripts/gen-icons.mjs",
    "cap:sync": "npm run build && cap sync",
    "cap:android": "npm run build && cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.2",
    "typescript": "^5.9.3",
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.20.0"
  }
}
